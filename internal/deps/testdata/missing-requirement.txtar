A file importing a module the module file does not require, which is a compile
error and so a load that fails rather than a dependency answer.

-- go.mod --
module example.com/app

go 1.27.1
-- app.go --
package app

import "example.com/absent/lib"

// Run calls a module nothing requires.
func Run() string { return lib.Name() }
